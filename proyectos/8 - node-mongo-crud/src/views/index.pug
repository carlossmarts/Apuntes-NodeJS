extends ./template/template
prepend content
    .container.text-center
        .row
            .col-md-8.offset-2.text-info
                a(href='/add' type="button" data-toggle="modal" data-target="#addModal")
                    i.fas.fa-plus-circle   Nueva tarea
        .row
            .col-md-8.offset-2
                //tabla
                table.table.table-bordered.table-hover
                    thead
                        tr
                            th N°
                            th Titulo
                            th Descripcion
                            th Acciones
                    tbody
                        - let i = 1
                        each task in tasks
                            tr
                                td= `${i++}`
                                td= task.title
                                td= task.description
                                td
                                    .row.justify-content-around
                                        a.btn.btn-sm.respButton(href="/done/" + task._id class= !task.done? "btn-dark" : "btn-success") Hecho
                                        a.btn.btn-danger.btn-sm.respButton(href="/delete/"+task._id) Eliminar
                                        a.btn.btn-info.btn-sm.respButton(type="button" data-toggle="modal" data-target="#updModal") Modificar
                                        include ./partials/updModal
    include ./partials/addModal
    

                    


                